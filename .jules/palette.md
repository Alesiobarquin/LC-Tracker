## 2026-06-24 - Contextual ARIA labels in repetitive mapped lists
**Learning:** Contextual ARIA labels in repetitive mapped lists are necessary to avoid screen reader ambiguity.
**Action:** Add contextual titles and ARIA labels (e.g. `Start session for ${prob.title}`) to interactive components inside mapped lists.
