1. **Import `createPortal`** in `src/components/Layout.tsx` from `react-dom`.
2. **Wrap modals with `createPortal`** in `src/components/Layout.tsx` (using `document.body` as the target).
3. **Verify** that the app compiles and `pnpm test`/`pnpm lint` pass.
4. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
